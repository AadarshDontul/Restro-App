spring:
  application:
    name: Restaurant-App
  datasource:
    url: jdbc:postgresql://localhost:5431/restro
    username: postgres
    password: mysecretpassword
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
